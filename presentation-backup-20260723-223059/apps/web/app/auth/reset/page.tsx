import { requestReset } from './actions';
export default async function ResetPage({ searchParams }: { searchParams: Promise<Record<string,string|undefined>> }) { const params = await searchParams; return <main className="shell"><section className="form-card"><h2>Reset your password</h2><p style={{color:'var(--muted)'}}>We’ll send a secure reset link to your verified email.</p>{params.error&&<p className="notice">{params.error}</p>}{params.message&&<p>{params.message}</p>}<form className="form" action={requestReset}><label className="field">Email<input name="email" type="email" required /></label><button className="button">Send reset link</button></form></section></main> }
