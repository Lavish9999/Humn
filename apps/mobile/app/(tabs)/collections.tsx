import {useEffect,useState} from 'react';import {FlatList,SafeAreaView,Text,View} from 'react-native';import {mobileSupabase} from '@human/database/mobile';import {colors,spacing} from '../../src/theme';export default function Collections(){const[data,setData]=useState<any[]>([]);useEffect(()=>{mobileSupabase.from('collections').select('*').order('updated_at',{ascending:false}).then(r=>setData(r.data??[]))},[]);return <SafeAreaView style={{flex:1,backgroundColor:colors.background}}><View style={{padding:spacing.lg}}><Text style={{fontSize:34,fontWeight:'800'}}>Collections</Text></View><FlatList data={data} keyExtractor={x=>x.id} renderItem={({item})=><View style={{padding:spacing.lg,borderBottomWidth:1,borderColor:colors.border}}><Text style={{fontWeight:'800'}}>{item.name}</Text><Text style={{color:colors.textSecondary}}>{item.privacy}</Text></View>} ListEmptyComponent={<Text style={{padding:spacing.lg,color:colors.textSecondary}}>Save work that belongs together.</Text>}/></SafeAreaView>}
