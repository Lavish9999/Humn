import { requestReset } from './actions';
export default async function ResetPage({ searchParams }: { searchParams: Promise<Record<string,string|undefined>> }) { const params = await searchParams; return <main className="shell"><section className="form-card"><header className="form-header"><div className="eyebrow">Account recovery</div><h3>Reset your password</h3><p className="muted">We’ll send a secure reset link to your verified email.</p>{params.error&&<p className="notice">{params.error}</p>}{params.message&&<p>{params.message}</p>}</header><div className="form-body"><form className="form" action={requestReset}><label className="field"><span className="field-label">Email</span><input name="email" type="email" required /></label><button className="button primary">Send reset link</button></form></div></section></main> }
